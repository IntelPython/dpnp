name: Coverage specific packages
channels:
  - conda-forge
dependencies:
  - python=3.12 # no python 3.13 support by coveralls
  - coverage[toml]
  - llvm
  - pytest-cov
  - pip:
    - -r coverage.txt
