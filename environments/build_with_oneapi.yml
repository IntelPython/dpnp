name: Packages to build DPNP with OneAPI env activated
channels:
  - dppy/label/dev
  - conda-forge
dependencies:
  - cmake
  - cython
  - ninja
  - numpy
  - pytest
  - scikit-build
  - pip:
    - dpctl==0.20.0dev0
